# Core backend dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
numpy>=1.24.0
Pillow>=10.0.0

# BGE embedding (optional, controlled by BGE_VL_ENABLE=0 to skip)
sentence-transformers>=2.2.0
torch>=2.0.0

# PCA projection (optional, graceful fallback if missing)
scikit-learn>=1.3.0

# Vector database for dataset/embedding storage
pymilvus>=2.4.0

# Benchmark / export scripts (optional)
onnxruntime>=1.16.0
transformers>=4.35.0
infinity-emb>=0.0.30
nest-asyncio>=1.5.0
